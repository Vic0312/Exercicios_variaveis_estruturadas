// Exercicio 07
using System;

namespace Exercicios_variaveis_estruturadas
{
    class Ex07
    {
        /*static void Main(string[] args){

            int n, par, soma, mult;
            
            Console.WriteLine("Digite o tamanho do vetor: ");
            n = Convert.ToInt32(Console.ReadLine());

            int[] vetor = new int[n];
            par = 0;
            soma = 0;
            mult = 1;

            for(int i=0; i < vetor.Length; i++)
            {
                Console.WriteLine("Digite o valor de um elemento do vetor: ");
                vetor[i] = Convert.ToInt32(Console.ReadLine());
            }

            Console.WriteLine("-----------");

            for(int i=0; i < vetor.Length; i++)
            {
                if(vetor[i]%2==0)
                {
                    par++;
                    soma += vetor[i];
                    mult *= vetor[i];

                }
            }


            if(par == 0)
            {
                Console.WriteLine("Não há números pares para multiplicar.");
            }
            else
            {
                Console.WriteLine("Quantidade de números pares no vetor: " + par);
                Console.WriteLine("Soma dos números pares: " + soma);
                Console.WriteLine("Multiplicação dos números pares: " + mult);
            }

        }*/
    }
}


/*
TESTE DE MESA

 n  |vetor[i]|   Imprime
----------------------
 4  |   2   |   Quantidade = 2
    |   4   |   Soma = 6
    |   1   |   Multiplicação = 8
    |   5   |
*/
